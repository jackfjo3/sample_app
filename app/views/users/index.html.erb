<%= provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

	<ul class="users">
		<%= render @users %>  <!-- Rails inferes is list of User objects, iterates through and renders with _user.html.erb partial -->
	</ul>

<%= will_paginate %>